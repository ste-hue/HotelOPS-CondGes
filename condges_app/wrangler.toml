name = "condges-dashboard"
compatibility_date = "2024-01-01"

[build]
command = "pip install -r requirements.txt"

[env.production.vars]
FLASK_ENV = "production"

# Per usare con il tuo dominio personalizzato
# Aggiungi il tuo dominio nelle routes dopo il deploy iniziale
